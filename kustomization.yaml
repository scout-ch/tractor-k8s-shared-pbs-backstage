apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: backstage

resources:
  - namespace.yaml
  - backstage.yaml

# secretGenerator:
#   - name: backstage-secrets
#     literals:
      # Add your secrets here as key=value pairs
      # Example: GITHUB_TOKEN=your-token-here
      # These will be automatically base64-encoded by Kustomize
